---
phase: 01-local-gate-infrastructure
plan: 04
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - .beads/formulas/mol-c4flow-task.formula.yaml
autonomous: false
requirements:
  - INFR-05

must_haves:
  truths:
    - "A beads molecule formula template exists with explicit review and verify gate steps"
    - "Running bd mol pour with the formula creates a task molecule with gate beads that block advancement"
    - "The formula passes bd cook --dry-run validation"
  artifacts:
    - path: ".beads/formulas/mol-c4flow-task.formula.yaml"
      provides: "Beads molecule formula template with quality gate steps"
      contains: "mol-c4flow-task"
  key_links:
    - from: ".beads/formulas/mol-c4flow-task.formula.yaml"
      to: "bd mol pour"
      via: "formula is consumed by beads cook/pour commands"
      pattern: "mol-c4flow-task"

# NOTE: This plan has a blocking human checkpoint (Task 2). Plans 02 and 03
# are fully autonomous and run in the same wave. The executor SHOULD complete
# Plans 02 and 03 before presenting Plan 04's human verification checkpoint,
# so that the human gate does not block autonomous work in this wave.
---

<objective>
Create and validate the beads molecule formula template with quality gate steps for repeatable c4flow workflows.

Purpose: This formula gives developers a repeatable pattern — `bd mol pour mol-c4flow-task --var task_name="..."` — that creates a task with built-in review and verify gate steps. This is the "formula gate" path (vs dynamic gate creation in the review skill).
Output: Validated .beads/formulas/mol-c4flow-task.formula.yaml
</objective>

<execution_context>
@/home/tunn/.claude/get-shit-done/workflows/execute-plan.md
@/home/tunn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-local-gate-infrastructure/01-RESEARCH.md

<interfaces>
<!-- From 01-RESEARCH.md (formula structure — MEDIUM confidence, needs dry-run validation) -->

Proposed formula structure:
```yaml
id: mol-c4flow-task
title: "C4Flow Task: {{task_name}}"
description: "Standard c4flow task with quality gates"
variables:
  task_name:
    description: "Name of the task being implemented"
    required: true
steps:
  - id: implement
    title: "Implement: {{task_name}}"
    type: task
  - id: review-gate
    title: "Quality gate: Codex review"
    type: gate
    gate:
      type: human
      description: "Run c4flow:review. This gate auto-resolves when all checks pass."
    depends_on: [implement]
  - id: verify-gate
    title: "Quality gate: bd preflight"
    type: gate
    gate:
      type: human
      description: "Run c4flow:verify. This gate auto-resolves when preflight passes."
    depends_on: [review-gate]
```

NOTE: This YAML structure is MEDIUM confidence — inferred from help text. The exact schema for `type: gate` and `gate:` fields MUST be validated with `bd cook --dry-run` before considering this plan complete.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create and validate beads molecule formula template</name>
  <files>.beads/formulas/mol-c4flow-task.formula.yaml</files>
  <action>
**This task requires empirical validation because the formula YAML schema is MEDIUM confidence.**

**Step 1: Create directory structure**
- `mkdir -p .beads/formulas/`

**Step 2: Explore beads formula schema**
- Run `bd formula --help` and `bd cook --help` to confirm available flags
- If there are example formulas at `~/.beads/formulas/` or in the beads source, read them to understand the actual YAML structure
- Run `bd formula list` to see if any formulas already exist that can be used as reference

**Step 3: Create minimal test formula**
- Start with the simplest possible formula (just one task step, no gates) and validate it works:
  ```bash
  bd cook --dry-run .beads/formulas/mol-c4flow-task.formula.yaml
  ```
- If dry-run fails, adjust the YAML structure based on the error messages
- Iterate until the basic structure validates

**Step 4: Add gate steps**
- Add the review-gate and verify-gate steps using the structure from RESEARCH.md
- Run `bd cook --dry-run` again to validate gate step syntax
- If `type: gate` is not the correct key, try alternatives: `kind: gate`, `bead_type: gate`, or check error messages for hints
- The gates must have `depends_on` fields linking them sequentially: implement -> review-gate -> verify-gate

**Step 5: Test with pour**
- Run `bd mol pour mol-c4flow-task --var task_name="test-formula-validation" --dry-run` (if dry-run supported on pour)
- Or create a test pour in a temp directory and verify the resulting beads have gate properties
- Confirm that the poured molecule contains gate beads that would block `bd close` on the parent

**Step 6: Write final formula**
The formula must include:
- `id: mol-c4flow-task`
- `title` with `{{task_name}}` variable interpolation
- `description` explaining this is a c4flow task with quality gates
- `variables` section with `task_name` (required)
- `steps` with three beads: implement (task), review-gate (gate), verify-gate (gate)
- Gate descriptions referencing `c4flow:review` and `c4flow:verify` skills
- `depends_on` enforcing sequential flow: implement -> review -> verify

**If gate steps cannot be validated (schema differs significantly from expected):**
- Document the actual schema discovered
- Implement as close to the intended behavior as the beads schema allows
- Add a comment in the YAML explaining any deviations from the intended design
  </action>
  <verify>
    <automated>test -f .beads/formulas/mol-c4flow-task.formula.yaml && bd cook --dry-run .beads/formulas/mol-c4flow-task.formula.yaml 2>&1 | head -20 && echo "Formula file exists and cook attempted"</automated>
  </verify>
  <done>Formula file exists at .beads/formulas/mol-c4flow-task.formula.yaml, passes bd cook --dry-run validation, contains implement + review-gate + verify-gate steps with dependencies.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify formula template works with beads CLI</name>
  <files>.beads/formulas/mol-c4flow-task.formula.yaml</files>
  <action>
Human verification of the formula template. The formula YAML schema was MEDIUM confidence in research — the executor validated it empirically with bd cook --dry-run and may have adjusted the structure. User must confirm the formula works correctly before this plan is considered complete.

NOTE: Plans 02 and 03 (autonomous skills) should be completed before this checkpoint is presented to the user, so this human gate does not block autonomous work in Wave 2.

What was built: Beads molecule formula template with quality gate steps (implement -> review-gate -> verify-gate).

How to verify:
1. Review the formula file: `cat .beads/formulas/mol-c4flow-task.formula.yaml`
2. Confirm it has three steps: implement, review-gate, verify-gate
3. Run validation: `bd cook --dry-run .beads/formulas/mol-c4flow-task.formula.yaml`
4. Optionally test a pour: `bd mol pour mol-c4flow-task --var task_name="checkpoint-test"`
5. If poured, check that gate beads exist: `bd gate list --json`
6. Clean up test data if needed: `bd delete <test-bead-id>`

Resume signal: Type "approved" if formula works correctly, or describe issues to fix.
  </action>
  <verify>
    <automated>bd cook --dry-run .beads/formulas/mol-c4flow-task.formula.yaml 2>&1 | tail -5</automated>
  </verify>
  <done>User has verified the formula template works with beads CLI and approved it.</done>
</task>

</tasks>

<verification>
1. `.beads/formulas/mol-c4flow-task.formula.yaml` exists
2. `bd cook --dry-run` succeeds on the formula
3. Formula contains implement, review-gate, and verify-gate steps
4. Gate steps have depends_on fields enforcing sequential flow
5. User has verified the formula works in practice
</verification>

<success_criteria>
- Formula template is validated by beads CLI (not just assumed correct)
- Pouring the formula creates a task molecule with gate beads
- Gate beads would block bd close until resolved
- Any schema deviations from research are documented
</success_criteria>

<output>
After completion, create `.planning/phases/01-local-gate-infrastructure/01-04-SUMMARY.md`
</output>

---
phase: 04.1-standalone-beads-driven-c4flow-code
plan: "01"
type: execute
wave: 1
depends_on:
  - 04-02-PLAN.md
files_modified:
  - skills/code/SKILL.md
  - skills/c4flow/SKILL.md
  - skills/beads/SKILL.md
  - references/workflow-state.md
  - references/phase-transitions.md
  - .claude/tests/test-code-skill-file.sh
  - .claude/tests/test-c4flow-code-route.sh
  - .claude/tests/test-code-state-reference.sh
  - .claude/tests/test-code-fallbacks.sh
  - docs/README.codex.md
autonomous: false
requirements:
  - SKIL-04
  - SKIL-05
  - SKIL-06

must_haves:
  truths:
    - "`skills/code/SKILL.md` defines a standalone CODE workflow and no longer depends on `using-superpowers`, `using-git-worktrees`, or `subagent-driven-development`"
    - "`skills/code/SKILL.md` accepts implementation plans from `.planning/phases/...` or `docs/c4flow/plans/`, and missing-plan guidance points to `docs/c4flow/plans/`"
    - "`skills/code/SKILL.md` documents the concrete beads lifecycle: ready/show/claim/close/discovered-from/dolt-push"
    - "`skills/c4flow/SKILL.md`, `skills/beads/SKILL.md`, and `references/workflow-state.md` agree on the richer CODE state contract (`taskSource`, `taskQuery`, `taskIds`, `claimedTasks`, `implementationPlan`)"
    - "The CODE regression suite validates the standalone contract rather than the removed Superpowers dependency"
  artifacts:
    - path: "skills/code/SKILL.md"
      provides: "Standalone beads-driven CODE workflow"
      min_lines: 140
      contains: "docs/c4flow/plans/"
    - path: "skills/c4flow/SKILL.md"
      provides: "Top-level CODE routing that references standalone state fields"
      contains: "implementationPlan"
    - path: "skills/beads/SKILL.md"
      provides: "BEADS-to-CODE state handoff contract"
      contains: "taskSource"
    - path: "references/workflow-state.md"
      provides: "Updated CODE state schema"
      contains: "claimedTasks"
    - path: ".claude/tests/test-code-skill-file.sh"
      provides: "Standalone CODE skill contract test"
      contains: "docs/c4flow/plans/"
  key_links:
    - from: "skills/c4flow/SKILL.md"
      to: "skills/code/SKILL.md"
      via: "CODE-state dispatch instructions"
      pattern: "Load the c4flow:code skill"
    - from: "skills/beads/SKILL.md"
      to: "docs/c4flow/.state.json"
      via: "task handoff fields recorded for CODE"
      pattern: "taskSource"
    - from: "skills/code/SKILL.md"
      to: "docs/c4flow/plans/"
      via: "local implementation-plan recovery path"
      pattern: "docs/c4flow/plans/"
---

<objective>
Replace the broken Phase 4 Superpowers dependency with a standalone `c4flow:code` workflow that executes from local plans and a concrete bd lifecycle.

Purpose: the updated Phase 4 research showed that the old CODE path only delegated on paper. This follow-up wave makes CODE executable in this repository by removing unavailable skill dependencies, moving the local plan contract to `docs/c4flow/plans/`, and recording enough task metadata for deterministic resume and closure.

Output: updated skill/reference/test contracts plus a completed execution summary for the inserted Phase 4.1 repair.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-add-superpowers-and-subagent-driven-development-into-c4flow-code-skill-as-part-of-the-workflow/04-RESEARCH.md
</execution_context>

<context>
@skills/code/SKILL.md
@skills/c4flow/SKILL.md
@skills/beads/SKILL.md
@references/workflow-state.md
@references/phase-transitions.md
@.claude/tests/test-code-skill-file.sh
@.claude/tests/test-c4flow-code-route.sh
@.claude/tests/test-code-state-reference.sh
@.claude/tests/test-code-fallbacks.sh

<interfaces>
From the 2026-03-16 Phase 4 research addendum:
- the old CODE path delegated to unavailable skills
- `.state.json` persisted only `beadsEpic`, not active CODE task identity
- the regression suite protected wording, not real execution discipline

From the live repo contract:
- local implementation plans already exist under `docs/c4flow/plans/`
- `.planning/phases/.../*-PLAN.md` is also a valid execution input
- CODE → TEST must remain gated on all assigned work being closed via bd or `tasks.md`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite `c4flow:code` as a standalone beads-driven workflow</name>
  <files>skills/code/SKILL.md</files>
  <action>
Replace the current Phase 4 Superpowers coordinator with a standalone CODE workflow.

The rewritten skill must:
- describe CODE as Phase 3 implementation owned by C4Flow itself
- read `docs/c4flow/.state.json` and validate `currentState`
- accept task sources from bd or `docs/specs/<feature>/tasks.md`
- accept implementation plans from `.planning/phases/...` and `docs/c4flow/plans/`
- send missing-plan recovery to `docs/c4flow/plans/YYYY-MM-DD-<feature>.md`
- document the concrete bd lifecycle: `bd ready --json`, `bd show <id> --json`, `bd update <id> --claim --json`, `bd close <id> --reason ... --json`, discovered follow-up work, and `bd dolt push`
- keep CODE → TEST contingent on closed assigned work
- include direct-invocation recovery guidance and manual fallback guidance

The final file must not mention `using-superpowers`, `using-git-worktrees`, or `subagent-driven-development` as required dependencies.
  </action>
  <acceptance_criteria>
    - `skills/code/SKILL.md` contains `docs/c4flow/plans/`
    - `skills/code/SKILL.md` contains `bd update <id> --claim --json`
    - `skills/code/SKILL.md` contains `taskSource`
    - `skills/code/SKILL.md` does not contain `using-superpowers`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Align BEADS, orchestrator, and reference state contracts</name>
  <files>skills/c4flow/SKILL.md, skills/beads/SKILL.md, references/workflow-state.md, references/phase-transitions.md</files>
  <action>
Update the surrounding contracts so standalone CODE is internally consistent.

Required outcomes:
- `skills/c4flow/SKILL.md` initial state JSON includes `taskSource`, `taskQuery`, `taskIds`, `claimedTasks`, `implementationPlan`
- the CODE branch checks those fields during resume
- `skills/beads/SKILL.md` tells the operator to persist the BEADS-to-CODE handoff fields, not only `beadsEpic`
- `references/workflow-state.md` documents the richer state schema and maps CODE to the standalone `skills/code/SKILL.md`
- `references/phase-transitions.md` keeps the CODE → TEST gate tied to closed work, with wording updated for the standalone flow
  </action>
  <acceptance_criteria>
    - `skills/c4flow/SKILL.md` contains `taskSource`
    - `skills/beads/SKILL.md` contains `claimedTasks`
    - `references/workflow-state.md` contains `implementationPlan`
    - `references/phase-transitions.md` contains `closed after \`c4flow:code\` executes the plan`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Re-baseline the CODE regression suite to the new standalone contract</name>
  <files>.claude/tests/test-code-skill-file.sh, .claude/tests/test-c4flow-code-route.sh, .claude/tests/test-code-state-reference.sh, .claude/tests/test-code-fallbacks.sh</files>
  <action>
Replace the old Superpowers-specific assertions with standalone CODE assertions.

The suite should validate:
- local plan location under `docs/c4flow/plans/`
- standalone beads lifecycle commands
- richer state fields in the orchestrator/reference docs
- removal of the old dependency strings

Keep the same four test files and `run-code-skill-tests.sh` runner.
  </action>
  <acceptance_criteria>
    - all four test files reference the standalone contract
    - `bash .claude/tests/run-code-skill-tests.sh` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run after implementation:

```bash
bash .claude/tests/run-code-skill-tests.sh
rg -n "docs/c4flow/plans/|taskSource|claimedTasks|implementationPlan" skills/code/SKILL.md skills/c4flow/SKILL.md skills/beads/SKILL.md references/workflow-state.md
rg -n "using-superpowers|subagent-driven-development|using-git-worktrees" skills/code/SKILL.md
```
</verification>
